{
  "name": "covenantpulse",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/desktop-electron",
    "build": "npm run build -w apps/desktop-electron",
    "package": "npm run package -w apps/desktop-electron",
    "test": "npm run test -w apps/desktop-electron"
  }
}
